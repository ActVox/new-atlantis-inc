# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.DS_Store

# Environment files — never commit real secrets.
# .env.example is the tracked template and is negated below.
.env
.env.*
!.env.example

# Build artifacts
/out/
next-env.d.ts
*.tsbuildinfo
.vercel/
